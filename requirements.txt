# TensorFlow 2.11 requires NumPy < 1.24 to avoid "AttributeError: module numpy has no attribute typeDict"
numpy<1.24

# Pinned TensorFlow (Compatible only with Python 3.7–3.10)
tensorflow==2.11.0
deepface

# Let the server resolve the correct headless version for the OS
opencv-python-headless

# Streamlit dependencies
streamlit
streamlit-webrtc
aiortc
joblib
scikit-learn
pandas
