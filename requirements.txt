# --- Core Libraries ---
numpy
pandas
scikit-learn
joblib
streamlit
streamlit-webrtc

# --- Computer Vision ---
opencv-python
deepface

# --- TensorFlow and Keras Compatibility (The final fix) ---
# Ensure the version that the environment uses is listed
tensorflow==2.20.0 
# Install the core Keras 3 library
keras
# Install the exact compatibility package requested by the error
tf-keras 
# Protobuf constraint is necessary for deepface's dependencies
protobuf<5.0
