# --- Core Libraries ---
numpy
pandas
scikit-learn
joblib
streamlit
streamlit-webrtc

# --- Computer Vision ---
opencv-python
deepface

# --- Deep Learning (Keras 2 Compatibility) ---
# TensorFlow 2.15 is the last version that natively supports Keras 2.x imports (tf.keras)
tensorflow==2.15.0
# Protobuf version is set to a stable pre-v5 version to satisfy deepface dependencies (which still rely on mediapipe/older Keras).
protobuf<5.0
