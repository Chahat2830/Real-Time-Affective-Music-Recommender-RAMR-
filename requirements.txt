# --- Core Libraries ---
numpy
pandas
scikit-learn
joblib
streamlit
streamlit-webrtc

# --- Computer Vision ---
opencv-python
deepface

# --- TensorFlow and Keras Compatibility (Direct Fix) ---
tensorflow==2.20.0 
# Install the core Keras library (Keras 3)
keras
# Install the compatibility package explicitly requested by the error message
tf-keras 
# Protobuf is constrained to satisfy deepface's mediapipe dependencies
protobuf<5.0
